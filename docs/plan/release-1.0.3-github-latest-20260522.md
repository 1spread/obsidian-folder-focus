# Release 1.0.3 GitHub latest plan

- [x] `1spread/obsidian-folder-focus` の現在の GitHub Release / tag 状態を確認する
- [x] ローカル `main` と `1spread/main` の同期状態を確認する
- [x] `npm run build` と `git diff --check` で release 前検証を行う
- [x] この計画ファイルを commit し、`1spread/main` に push する
- [x] `1.0.3` tag を `main` に作成して `1spread` に push する
- [x] GitHub Actions release workflow の完了を確認する
- [x] GitHub Releases で `1.0.3` が Latest になっていることを確認する
