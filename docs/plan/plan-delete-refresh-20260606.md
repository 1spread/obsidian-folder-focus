# Delete refresh

- [x] 現状の vault change listener と `deleteItem` の挙動を確認する
- [x] 削除イベントで `parent` が欠けても現在フォルダを判定できるようにする
- [x] Folder Focus から削除した直後に表示を更新する
- [x] build/test で確認する

## Verification

- `npm run test`
- `npm run build`
