# Release 1.0.3 Finder import plan

- [x] `manifest.json` / `package.json` / `versions.json` を `1.0.3` に更新する
- [x] `CHANGELOG.md` にFinder drag and drop importを追記する
- [x] `npm run build` と `git diff --check` で検証する
- [ ] 変更をcommitしてIGNITE remoteへpushする
- [x] ローカルObsidian vaultのpluginフォルダへ `main.js` / `manifest.json` / `styles.css` を反映する
- [x] Obsidian側で有効化状態を確認する
