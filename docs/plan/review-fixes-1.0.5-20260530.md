# Review fixes 1.0.5

- [x] review warning の対象箇所を確認する
- [x] `styles.css` の `!important` を selector specificity で置き換える
- [x] README の repository URL を `1spread/obsidian-folder-focus` に修正する
- [x] `npm run test` / `npm run build` / `git diff --check` で検証する
- [ ] warning fix を commit / push する
- [x] 必要なら `1.0.5` release として Obsidian review に出せる状態にする
